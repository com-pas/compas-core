## YAML Template.
---
spring:
  #profile
  profiles:
    active: test

compas:
  scl:
    schema:
      paths: ## Define all schemas (namespace) path here : (alias: "path")
        - scl: "classpath:schema/SCL.xsd"
